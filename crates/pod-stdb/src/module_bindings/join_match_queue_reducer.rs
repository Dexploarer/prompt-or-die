// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct JoinMatchQueueArgs {
    pub entity_id: u64,
    pub desired_party_size: u32,
}

impl From<JoinMatchQueueArgs> for super::Reducer {
    fn from(args: JoinMatchQueueArgs) -> Self {
        Self::JoinMatchQueue {
            entity_id: args.entity_id,
            desired_party_size: args.desired_party_size,
        }
    }
}

impl __sdk::InModule for JoinMatchQueueArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `join_match_queue`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait join_match_queue {
    /// Request that the remote module invoke the reducer `join_match_queue` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`join_match_queue:join_match_queue_then`] to run a callback after the reducer completes.
    fn join_match_queue(&self, entity_id: u64, desired_party_size: u32) -> __sdk::Result<()> {
        self.join_match_queue_then(entity_id, desired_party_size, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `join_match_queue` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn join_match_queue_then(
        &self,
        entity_id: u64,
        desired_party_size: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl join_match_queue for super::RemoteReducers {
    fn join_match_queue_then(
        &self,
        entity_id: u64,
        desired_party_size: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            JoinMatchQueueArgs {
                entity_id,
                desired_party_size,
            },
            callback,
        )
    }
}
